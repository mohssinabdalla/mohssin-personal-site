[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[context.branch-deploy]
  command = "npm run build"
  publish = "dist"

[context.dev]
  command = "npm run build"
  publish = "dist"

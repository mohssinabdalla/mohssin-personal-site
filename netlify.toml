[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Branch deploy previews — every push to any branch gets its own preview URL
[[context.branch-deploy]]
  command = "npm run build"

# Dev branch specific
[[context.dev]]
  command = "npm run build"
